document.addEventListener("DOMContentLoaded", () => {
  const el = document.querySelector('[data-label="header"]');
  if (el) {
    console.log("Header logic running", el);
    // Add nav menu logic, scroll detection, etc.
  }
});
