{
  "name": "unnamed-v2",
  "version": "2.0.0",
  "main": "index.js",
  "scripts": {
    "start": "concurrently --kill-others \"npm run dev\" \"npm run watch\" \"npm run browser-sync\"",
    "dev": "shopify theme dev",
    "watch": "nodemon -e js,ts,html,scss,css --ignore assets --exec 'npm run build'",
    "build": "vite build",
    "browser-sync": "browser-sync start --files 'assets/*.*' --no-open --proxy 'http://127.0.0.1:9292'",
    "shopify: pull": "shopify theme pull -d -e development",
    "shopify: pull--local": "shopify theme pull -d"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "chokidar": "^4.0.3",
    "concurrently": "^9.2.0",
    "lodash": "^4.17.21",
    "nodemon": "^3.1.10",
    "sass": "^1.90.0",
    "vite": "^7.0.6"
  },
  "dependencies": {
    "lenis": "^1.3.8",
    "scss": "^0.2.4"
  }
}
